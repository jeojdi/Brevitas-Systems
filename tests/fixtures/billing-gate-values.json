{
  "$comment": [
    "U4 (docs/STRIPE_TEST_PLAN.md §2.1) — the single source of truth for what",
    "BREVITAS_BILLING_ENABLED values mean 'billing is on'.",
    "",
    "Two independent implementations read this variable and MUST agree, because",
    "they gate different halves of the same money path:",
    "  * src/lib/billing/maintenance-gate.mjs:4 gates the Next.js routes",
    "    (checkout, portal, sync) with a 503 before any authentication.",
    "  * api/billing_recovery.py:834 (billing_recovery_is_configured) gates the",
    "    worker that actually posts meter events to Stripe.",
    "  * src/lib/billing/config.ts:16 derives config.enabled the same way.",
    "A disagreement is not a cosmetic bug: if the worker considered ' true ' to",
    "be enabled while the routes did not, meter events would be billed for a",
    "deployment whose UI still says billing is off — or the reverse, a customer",
    "could be sent to Checkout by a surface whose worker will never bill.",
    "",
    "`absentIsDisabled` covers the unset variable, which JSON cannot express as",
    "a list entry. Every entry of `disabledValues` is a REALISTIC mistake:",
    "casing, a shell quoting accident, a YAML boolean, or the leading/trailing",
    "whitespace that a copy-paste out of a dashboard or a .env comment leaves",
    "behind. `' true '` and `'true '` are called out by name in the test plan."
  ],
  "variable": "BREVITAS_BILLING_ENABLED",
  "enabledValue": "true",
  "absentIsDisabled": true,
  "disabledValues": [
    "",
    "false",
    "FALSE",
    "False",
    "True",
    "TRUE",
    "0",
    "1",
    "yes",
    "on",
    "enabled",
    "t",
    " true ",
    "true ",
    " true",
    "true\n",
    "\ttrue",
    "TRUE ",
    "true;",
    "\"true\"",
    "'true'"
  ]
}
