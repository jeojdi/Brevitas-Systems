import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Middleware handles routing to static HTML files
  // API routes are handled by Next.js App Router
};

export default nextConfig;
