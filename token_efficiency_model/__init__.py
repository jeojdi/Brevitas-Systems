"""Brevitas token-efficiency engine.

The live engine is `token_efficiency_model.lossless` — faithful implementations of published
algorithms (IPFS/LBFS dedup, Myers/VCDIFF/rsync delta, DPR/ColBERTv2 retrieval, RLM,
provider-native caching). The previous lossy stack has been archived under `archive/`.
"""
